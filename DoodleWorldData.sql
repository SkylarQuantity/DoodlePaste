CREATE DATABASE DoodleWorldData

CREATE TABLE Doodles(
DoodleName varchar(255) NOT NULL,
PrimaryType varchar(255) NOT NULL
);

CREATE TABLE Moves(
    MoveName varchar(255) NOT NULL,
    MoveType varchar(255) NOT NULL
);